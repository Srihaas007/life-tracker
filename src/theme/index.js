import { colors } from './colors';
import { fonts, fontSizes, textStyles } from './typography';
import { spacing, borderRadius, shadows } from './spacing';

export const theme = {
    colors,
    fonts,
    fontSizes,
    textStyles,
    spacing,
    borderRadius,
    shadows,
};

export { colors, fonts, fontSizes, textStyles, spacing, borderRadius, shadows };
